1. autoscan
	cp configure.scan configure.ac
	vim configure.ac修改内容，具体见文件

2. aclocale

3. autoheader

4. autoconf生成configure
   更新生成的配置:autoreconf --install

5. vim Makefile.am

6. automake

7. ./configure
   ./configure LDFLAGS=-static # 静态编译，默认使用共享库
	
